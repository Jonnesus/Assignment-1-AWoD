using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class STUFFTODO : MonoBehaviour
{
    /* Fix DeathWall, Spikes + Portal collision
     * Set up UI
     * Main menu
     * Background?
     * Procedural generation (ONLY when all else is fixed)
     */
}
